{{ header }}
<main  class="offcanvas-enabled">


  <!-- Page Title-->
  <div class="page-title-overlap bg-dark pt-4 ps-lg-4 pe-lg-3">
    <div class="container d-lg-flex justify-content-between py-2 py-lg-3 ">
      <div class="order-lg-2 mb-3 mb-lg-0 pt-lg-2">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb breadcrumb-light flex-lg-nowrap justify-content-center justify-content-lg-start">
            {% for breadcrumb in breadcrumbs %}
              <li class="breadcrumb-item"><a class="text-nowrap" href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
            {% endfor %}
          </ol>
        </nav>
      </div>
      <div class="order-lg-1 pe-lg-4 text-center text-lg-start">
        <h1 class="h2 text-light mb-0">{{ heading_title }}</h1>
      </div>
    </div>
  </div>



  <div id="content" class="container pb-5 mb-2 mb-md-4 ps-lg-4 pe-lg-3">
    <div class="row">
      <!-- Sidebar-->

      {{ column_left }}

      <!-- Content  -->
      <section class="col-lg-8" >

        <!-- Toolbar-->
        <div class="d-none d-lg-flex justify-content-between align-items-center pt-lg-3 pb-4 pb-lg-5 mb-lg-3">
          <h6 class="fs-base text-light mb-0">{{ heading_title }}</h6><a class="btn btn-primary " href="{{ logout }}"><i class="fal fa-sign-out me-2"></i>{{ text_logout }}</a>
        </div>
        {% if success %}
          <div class="alert alert-success"><i class="fal fa-check-circle"></i> {{ success }}</div>
        {% endif %}

        {% if customers %}
        <!-- Orders list-->
        <div class="table-responsive fs-md mb-4">
          <table class="table table-hover mb-0">
            <thead>
            <tr>
              <th>Rbr.</th>
              <th>Kupac</th>
              <th>Email</th>
              <th>Telefon</th>
              <th>Kreiran</th>
              <th></th>
            </tr>
            </thead>
            <tbody>

            {% if customers %}
            {% set count = 0 %}
            {% for customer in customers %}
              {% set count = count + 1 %}
              <tr>
                <td class="py-3"> {{ count }}  </td>
                <td class="py-3">{{ customer.firstname }} </td>
                <td class="py-3">{{ customer.email }}</td>
                <td class="py-3">{{ customer.telephone }}</td>
                <td class="py-3">{{ customer.date_added }}</td>

                <td class="py-3">
                  <form action="{{ customer.edit }}" method="post">
                    <button style="background: unset;border: unset;"><i class="fal fa-edit"></i></button>
                    <input type="hidden" name="customer_id" value="{{ customer.customer_id }}" />
                    <input type="hidden" name="not" value="{{ customer.not }}" />
                  </form>
                </td>

              </tr>
            {% endfor %}
            {% endif %}
            </tbody>
          </table>
        </div>

          <div class="col-12">

            <div class="d-flex flex-wrap justify-content-between align-items-center">

              <a class="btn btn-primary mt-3 mt-sm-0" href="{{ subaccount }}" >Dodaj novi račun</a>
            </div>
          </div>
        {% else %}
          <p>Trenutno nemate dodanih poslovnica.</p>
        {% endif %}



      </section>
     
      </div>
    </div>

{{ footer }}